# TCLP Risk ID Environment Configuration

# API Keys (Required for clause recommendation feature)
OPENROUTER_API_KEY=your_openrouter_api_key_here

# LLM Configuration (Optional - defaults to free model)
OPENROUTER_MODEL=tngtech/deepseek-r1t2-chimera:free

# Base Path for Subfolder Deployment (handled by nginx reverse proxy)
# This is informational only - nginx strips this prefix before forwarding to the app
BASE_PATH=/risk_id

# Application Settings
TOKENIZERS_PARALLELISM=false
PYTHONPATH=/app
